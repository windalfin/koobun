<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ── Estate Tree View ──────────────────────────────── -->
    <record id="view_estate_estate_tree" model="ir.ui.view">
        <field name="name">estate.estate.tree</field>
        <field name="model">estate.estate</field>
        <field name="arch" type="xml">
            <list string="Estates">
                <field name="name"/>
                <field name="code"/>
                <field name="phone"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- ── Estate Form View ──────────────────────────────── -->
    <record id="view_estate_estate_form" model="ir.ui.view">
        <field name="priority">99</field>
        <field name="name">estate.estate.form</field>
        <field name="model">estate.estate</field>
        <field name="arch" type="xml">
            <form string="Estate">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object"
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                   options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived"
                            bg_color="bg-danger"
                            invisible="active"/>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                        </group>
                        <group>
                            <field name="phone"/>
                        </group>
                    </group>
                    <group string="Address">
                        <field name="address" nolabel="1"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ── Estate Action ─────────────────────────────────── -->
    <record id="action_estate_estate" model="ir.actions.act_window">
        <field name="name">Estates</field>
        <field name="res_model">estate.estate</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create the first estate!
            </p>
        </field>
    </record>
</odoo>
