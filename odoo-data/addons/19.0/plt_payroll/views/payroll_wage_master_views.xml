<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ── Wage Master Tree View ──────────────────────────── -->
    <record id="view_payroll_wage_master_tree" model="ir.ui.view">
        <field name="name">payroll.wage_master.tree</field>
        <field name="model">payroll.wage_master</field>
        <field name="arch" type="xml">
            <list string="Wage Masters">
                <field name="name"/>
                <field name="worker_class"/>
                <field name="daily_wage"/>
                <field name="hourly_rate"/>
                <field name="effective_from"/>
                <field name="effective_to"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-warning="state == 'draft'"
                       decoration-danger="state == 'expired'"/>
            </list>
        </field>
    </record>

    <!-- ── Wage Master Form View ──────────────────────────── -->
    <record id="view_payroll_wage_master_form" model="ir.ui.view">
        <field name="priority">99</field>
        <field name="name">payroll.wage_master.form</field>
        <field name="model">payroll.wage_master</field>
        <field name="arch" type="xml">
            <form string="Wage Master">
                <header>
                    <button name="action_approve" type="object"
                            string="Approve"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_set_draft" type="object"
                            string="Set Draft"
                            invisible="state not in ('approved', 'expired')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,approved,expired"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="worker_class"/>
                        </group>
                        <group>
                            <field name="effective_from"/>
                            <field name="effective_to"/>
                        </group>
                    </group>
                    <group string="Wage Amounts">
                        <group>
                            <field name="daily_wage"/>
                            <field name="hourly_rate"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ── Wage Master Action ─────────────────────────────── -->
    <record id="action_payroll_wage_master" model="ir.actions.act_window">
        <field name="name">Wage Masters</field>
        <field name="res_model">payroll.wage_master</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
